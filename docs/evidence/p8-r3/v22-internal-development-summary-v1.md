# P8-R3 V2.2 internal development screen — PASS

The prospectively frozen source/protocol commit was `65122a8` on `feature/p8-r3-resolution-independent-looming` (original fetched `origin/main` base `e4ade0276e5e3f67381f4ca3da9f181077387fa1`). I independently evaluated all **18** planned cases in [the raw record](v22-internal-development-raw-v1.json): seeds **885501–885503**, both **65×33** and **129×65** resolutions, and approach/static/receding trajectories at 20 Hz RGB with 50 Hz neural updates. The raw file is **562,760 bytes**, SHA256 `f5bd155a82c308f1d5eee8e95ca41e05ed009494f9795c910d44617d7a089f60`. It contains **546 visual frames** and **1,338 neural steps**. The [artifact manifest](v22-internal-artifact-manifest-v1.json) records hashes, byte counts, and record counts.

The preregistered internal gate **PASSES**: each resolution has **3/3** healthy DNp01 ≥0.5 and EscapeDecoder stops strictly before the 0.25 m virtual boundary, while **0/6** static/receding controls per resolution have any neural or fault stop. All neural steps are finite, runtime healthy, and watchdog healthy. Recomputed first-event records match the raw frame/step sequences; the maximum synthetic RGB interframe gap is **50 ms**, and maximum neural frame age is **40 ms**. The smallest first-stop boundary margin is **0.06623 m**.

| Seed | First positive looming, 65×33 / 129×65 (s) | First DNp01 ≥0.5, 65×33 / 129×65 (s) | Paired differences (looming / DN, ms) | LPLC2 peak ratio | First-stop margin, both resolutions (m) |
| ---: | --- | --- | ---: | ---: | ---: |
| 885501 | 2.05 / 2.05 | 2.12 / 2.12 | 0 / 0 | 1.000 | 0.27855 |
| 885502 | 2.65 / 2.65 | 2.72 / 2.72 | 0 / 0 | 1.000 | 0.15187 |
| 885503 | 3.05 / 3.05 | 3.12 / 3.12 | 0 / 0 | 1.000 | 0.06623 |

All paired timing differences are ≤100 ms; all nonzero LPLC2 peak ratios are within [0.75, 1.333]. The matched peak of 1.0 is saturated and does not demonstrate unsaturated amplitude invariance. This is fixed-pose, virtual-trajectory evidence only. It does not establish moving-body behavior, production visual freshness, robotd stop transport, the separate fault-stop gate, or official P8-R3/G8 success. The prior official V2.1 **0/3 FAIL** remains unchanged. Fresh official seeds **885521–885523** remain unused by this internal screen.

**Scenario file provenance limit:** the internal raw record identifies the Windows working-file bytes of `config/looming_scenario_v1.json`: **1,133 bytes**, SHA256 `0020354549e3bfcd6e91c82153af2a3db9b91a5522f70ea4d027148883eae735`. Those bytes contain 29 CRLF line endings. Replacing each CRLF with LF yields **1,104 bytes**, SHA256 `3967c2efa327c0a69a6f9629a12ac0c80f38a7ca6b02b4e9aceecd4cbd53c9eb`, exactly equal byte-for-byte to the file in frozen commit `65122a8` and the official pinned scenario hash. Parsed JSON is identical. Thus the scenario parameters are equivalent, but the raw internal scenario hash is **not** the committed file's byte hash. Keep both identities in provenance; do not relabel the internal raw hash or treat this synthetic screen as proof of official binary input identity.

The internal script directly loads that scenario file. Its `config/p8_r3_visual_scheduler_v1.json` was **not** an input to this synthetic run; the working copy is 239 CRLF bytes, SHA256 `852070e80957646de2a144a934420bec6b6caef1e536e77a28dbce0893869cb5`, and CRLF normalization exactly matches the committed/official pinned 229 LF bytes, SHA256 `6f12f48687327b18b897a6ca3b7b14c911f03339ac51756ae2b7fde68d62f769`. Official recertification must check its own clean, pinned file hashes and actual cadence; the internal 50 ms synthetic gaps cannot certify that runtime behavior. The successful run used Windows Python **3.13.15** and the exact command recorded in the manifest; an earlier invocation without `PYTHONPATH=.` failed import before seed execution.

The manifest's listed neural model, sensory mapping, escape decoder, safety envelope, and watchdog JSON hashes also identify Windows CRLF working copies. For every listed JSON config, CRLF-to-LF normalization is byte-identical to frozen commit `65122a8` and parsed JSON is equal; the manifest records both byte identities individually. This establishes parameter equivalence for this internal run, while the official run still requires fresh verification of its own pinned LF inputs.
