# P8-R3 V2.3 focused startup handoff gate — PASS

I independently checked all **11** cases in the frozen-source [local raw record](v23-startup-gate-raw-v1.json) and the [Thor Python 3.12 raw record](v23-startup-gate-thor-raw-v1.json) against the [prospective V2.3 protocol](../../tasks/P8-R3-V2_3-DEVELOPMENT-PROTOCOL.md). Both identify source `fde86b36d54ad652a8c93e3f043563a24e5b8fd6` and report PASS. The local file is **65,109 bytes**, SHA256 `7ba645f2d7ee14ee2faedd250d3c875113109886eec3aa2ce2374e7db9cd5035`, with **256 events**; Thor is **64,280 bytes**, SHA256 `7d4fc4200b680e949f976e1cf95486e480239de33eff3f8b19e188b8ece0dc31`, with **251 events**. The [manifest](v23-startup-gate-artifact-manifest-v1.json) records these and source/config byte identities.

| Case group | Local evidence | Thor evidence | Outcome |
| --- | --- | --- | --- |
| Healthy deterministic handoff | 12/12 neural inputs present; original frame ID/timestamp preserved; one direct priming update; max age 40 ms, max RGB gap 60 ms; zero dropped neural updates | Same | PASS |
| Real scheduler worker and stop refresh | 18/18 neural inputs present, max age **50.23 ms**, max RGB gap **60.67 ms**; 14 fake `robot.stop` ACKs, maximum ACK gap **31.14 ms**; zero post-stop moves or scheduler exceptions | 17/17 inputs, max age **40.53 ms**, max RGB gap **60.41 ms**; 13 stop ACKs, maximum gap **20.01 ms**; zero post-stop moves or scheduler exceptions | PASS |
| Missing/invalid/future/stale/malformed/NaN priming | All six cases rejected before neural ticks or robot-facing commands | Same | PASS |
| Delayed scheduler start | Rejected at 100 ms source age before scheduler ticks or robot-facing commands | Same | PASS |
| Deliberately dropped visual frames | Seven later neural inputs become `None`; authentic `stale_neural` safe stop occurs, followed by persistent `fault_stale_neural` stop; seven stop ACKs with 20 ms maximum gap and no move after first stop | Same | PASS as fault-injection safety case |
| Fault after healthy neural stop | Four stop ACKs; external `fault_camera_loss` supersedes healthy-neural latch reason, with no move after latch | Same | PASS |

The healthy handoff and worker paths have **zero** armed missing inputs, missing ages, or dropped neural updates. Frame ages use the original camera source timestamp, not cache-insertion time. Neural update sequences are contiguous, and the cache handoff adds no robot-facing command or second direct priming step. The deliberate visual-drop case is **not a healthy behavioral success**: its seven missing inputs and 260 ms RGB gap are retained as fault stimulus, with safe stop and no resumption. The protocol calls for a dropped-frame safe-stop test but also says “any skipped armed tick” fails the gate without expressly excluding injected faults. This PASS applies the zero-skip requirement to the healthy handoff paths and the explicit safe-stop criterion to the injected-fault path; a literal zero-skip requirement across fault injection would be contradictory and is a textual limitation to resolve in the official metric interpretation. Official V2.3 still requires zero skipped armed ticks in every non-injected trial.

All nine source/script/config hash entries in **each** raw record match frozen commit `fde86b3` after the recorded Windows CRLF-to-LF normalization; Thor execution hashes match committed LF bytes directly. The protocol and startup-gate script hashes also match the frozen commit. The local run used Python **3.13.15** with the exact command in the manifest; Thor used Python **3.12**. The Thor raw root for this gate is `/home/juper007/projects/microduck-connectome-thor/evidence/p8-r3-v23/startup-gate-thor-python312-v1.json`.

This focused gate uses a **fake robotd** with acknowledged high-level requests and synthetic neural readouts; it contains no actual MaleCNS graph computation, official robotd transport, MuJoCo body, or pose-derived stopped confirmation. The real scheduler worker timestamps and ACK gaps test the handoff timing in both environments, but official seeds **885621–885623** remain necessary for moving-body recertification. The retained V2.2 official **0/3 FAIL** is unchanged.
