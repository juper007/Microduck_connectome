# P8-R3 V2.4 group-separated startup gate — PASS

I independently recomputed all **11** preregistered cases per host from the frozen-source [local raw](v24-startup-gate-raw-v1.json) and [Thor raw](v24-startup-gate-thor-raw-v1.json), source `eea4eac492ed9210c7a756947f3f2e88e56410c3`. The local record is **72,381 bytes**, SHA256 `c2236ad27fc6d530b404eb589e5cff36be895aa9758318a8ae4fcb29873446f7`, with **262 events** on `Min-Desktop` / Python 3.13.15. Thor is **71,593 bytes**, SHA256 `7e133e748be2dbac9adfa6284dbc5570ebc148a666c151f691cc45c06b8b2a85`, with **257 events** on `jetsonthor01` / Python 3.12.3. The [artifact manifest](v24-startup-gate-artifact-manifest-v1.json) records bytes, SHA256, case/event counts, commands, and source/config identities.

| Frozen group | Local result | Thor result | Independent check |
| --- | --- | --- | --- |
| **A: healthy startup** (2 cases) | **2/2 PASS** | **2/2 PASS** | Deterministic path: 12/12 present valid neural inputs, maximum age **40 ms**, maximum RGB gap **60 ms**. Real worker: **18/18** local and **17/17** Thor present valid inputs; maximum age **62.61/40.71 ms**, maximum RGB gap **75.83/60.60 ms**. No dropped neural update, missing timestamp, scheduler exception, duplicate direct priming step, or command from cache insertion. Original first frame ID/timestamp survived cache priming; neural sequences were contiguous. Real-worker stop ACK refresh maximum gap **31.34/20.01 ms**, with no post-stop move. |
| **B: intentional fault safety** (9 cases) | **9/9 PASS** | **9/9 PASS** | Six absent/invalid/future/stale/malformed/NaN priming attempts and delayed start rejected before any robot-facing command. Sustained visual loss produced **seven** explicitly retained missing neural inputs on each host, then authentic `stale_neural` safe stop and persistent `fault_stale_neural` arbiter latch. Seven stop ACKs had **20 ms** maximum gap; zero post-latch move or scheduler exception. Two fresh frames and five neural updates after first stop did not resume motion. In the neural-then-fault case, `fault_camera_loss` superseded the healthy neural latch; all four ACKs remained stops. |

**Top-level V2.4 focused gate: PASS**, because Group A and Group B each pass their separate prospective criteria. The seven deliberately missing Group B ticks remain in the fault denominator and are **not** counted as healthy behavior or omitted from the record. The V2.3 exact-head preflight review **FAIL** and its earlier raw records remain unchanged.

For runtime reuse, I compared `fde86b36d54ad652a8c93e3f043563a24e5b8fd6` with `eea4eac492ed9210c7a756947f3f2e88e56410c3`. `microduck_connectome/neural_stop_scheduler.py`, the arbiter, Watchdog, fractional RGB renderer, looming estimator, and neural/escape/safety configs are byte-identical in Git. The changed official trial script adds V2.4 protocol/gate validation and row checks; the new V2.4 gate script/schema and official config are separately versioned. Each raw record's nine source/script/config committed hashes matches frozen `eea4eac`; local Windows execution copies normalize CRLF-to-LF exactly to those committed bytes, while Thor execution hashes equal the committed bytes directly. This supports reuse of the V2.3 priming runtime and unchanged V2.2 internal/fault results under the [V2.4 protocol](../../tasks/P8-R3-V2_4-DEVELOPMENT-PROTOCOL.md).

This is a **focused preflight** using a fake robotd that acknowledges high-level commands and synthetic neural readouts. The real scheduler worker was timed on each host, but no MaleCNS graph, official robotd transport, MuJoCo body, or pose-derived stopped confirmation was exercised. It cannot establish official moving-body success. Fresh official development seeds **885721–885723** remain unrun in this gate; P8-02 final and G8 remain unearned.
