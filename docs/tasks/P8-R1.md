---
task_id: P8-R1
owner_skill: behavior-control-engineer
support_skills: [microduck-integration-engineer, robot-safety-engineer, neural-runtime-engineer, experiment-evaluation-scientist, reproducibility-devops-engineer]
base_origin_main: e4ade0276e5e3f67381f4ca3da9f181077387fa1
branch: fix/p8-r1-neural-stop-persistence
worktree: C:\projects\Microduck_connectome\.worktrees\p8-r1-neural-stop-latch
phase: Phase 8 versioned remediation before P8-v2 preregistration
---

# P8-R1 — persist a healthy neural stop through body cessation

Historical development [P8 early-trigger probe draft PR #66](https://github.com/juper007/Microduck_connectome/pull/66)
(raw Thor evidence at
`/home/juper007/projects/microduck-connectome-thor/evidence/p8-v2-early-trigger-probe/dev-v3-666b3b1-20260925-2/`)
found that continuous visual input after the first healthy
graph-v2 DNp01/EscapeDecoder stop can yield a later non-stop Watchdog output.
The probe fixture rejected that attempted publish, stopped its `robot.stop`
refresh, and robotd deadman intervened before 200 ms pose-stop confirmation.
The same run's initial neural trigger was **late** at 0.211878 m; this task
does not recast it as an early looming success.

## Invariants

- Keep canonical graph-v2 SHA256
  `c4160c42941163079b6b569d117bf67afcf8b45eaa128c444f7c96c2567593cc`,
  LPLC2/DNp01 identities, decoder threshold, SafetyClamp limits, Watchdog
  100 ms TTL and 50 Hz scheduling, 0.25 m evaluator boundary, `robot.stop`,
  500 ms robotd deadman, and robotd motor authority unchanged.
- MaleCNS activity selects only high-level stop intent. No direct servo/zero
  twist/scripted/deadman substitute, fabricated WatchdogOutput, or raw
  robot.stop bypass of SafetyClamp/Watchdog/RobotMotionAdapter.
- Preserve the separate fault-stop latch introduced by PR #65; distinguish
  `healthy_neural_escape` from sensor/neural fault and shutdown stops.

## Work and acceptance

1. Provide a reusable high-level stop-intent latch. The first **healthy**
   DNp01/EscapeDecoder stop surviving SafetyClamp and healthy Watchdog wins;
   record its source, graph/runtime step, monotonic event time and original
   cause. Subsequent non-stop neural outputs are logged but cannot clear
   the stop before official pose-derived cessation. A fault stop has explicit
   priority and separate attribution.
2. Continue RGB/perception/neural execution after the first ACK (continuous
   approaching scene), while delivering authentic post-safety stop at the
   50 Hz control cadence. No new robot.move request after stop latch, no
   move ACK after first stop ACK, repeated accepted robot.stop ACK gap ≤100 ms
   until pose speed ≤0.008 m/s sustained 200 ms, applied vx declines, and
   no deadman limiter causes cessation before confirmation.
3. Add deterministic tests for latch/decoder transitions, concurrent
   motion-stop race, fault priority, stale updates, no post-stop move,
   scheduler lifetime, and unchanged safety bounds. Run relevant current-main
   regression on Thor Python 3.12.
4. Use official Thor duck-sim/robotd/MuJoCo with **new development-only seeds**
   outside P8-v2 final ranges. Log full RGB→LPLC2→graph-v2 DNp01→decoder→
   SafetyClamp→Watchdog→robot.stop request/ACK→applied velocity→pose chain.
   Keep the frozen 0.25 m boundary evaluator-only. A late trigger can prove
   transport persistence but remains a late behavioral failure.
5. Preserve every development attempt, including errors and failures, with
   path, bytes, record count, SHA256, source/config hashes, and simulator
   cleanup. Do not rewrite draft PR #66 or any earlier raw evidence.

Independent exact-head review is required before a PASS remediation PR can
merge. P8-v2 final protocol stays DRAFT until this and presentation
remediation are complete and independently reviewed.
