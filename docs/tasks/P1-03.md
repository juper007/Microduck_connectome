# P1-03 Task Packet

- Task: P1-03 Connectivity extraction, Phase 1. PM READY → IN_PROGRESS after merged P1-01/P1-02. No separately assigned READY issue exists.
- Owner: connectome-data-engineer; reviewer: independent-phase-reviewer.
- Base: freshly fetched origin/main `357a5e5751251bd09c8e942ea2269ee09e0b851a`, clean tree; new branch `feature/p1-03-connectivity-extraction`, isolated work/p1-03-connectivity-extraction.
- Goal: deterministic validated induced edge table for selected annotated body IDs, preserving exact raw synapse counts and full-source outgoing normalization.
- Read first: AGENTS and own skill; WBS P1-03; NEURAL_MODEL_SPEC §§4–5; DATA_AND_REPRODUCIBILITY_POLICY §§6–7; annotations.py and its documented artifact contract. Lazy context <25K, medium; no robot/perception docs or raw bulk data preload.
- Inputs: validated P1-02 annotation artifact defining selected nodes; explicit source edge rows body_pre/body_post/weight (positive int64 IDs/positive integer weights); pinned male-cns:v1.0 and explicit source hash, confidence/filter provenance, extraction code SHA. No biological type/side inference.
- Outputs: focused extraction module/API, boundary tests, usage/contract doc, compact validation handoff. Keep dependencies unchanged.
- Acceptance: sorted deterministic induced edges with source/target IDs, exact raw weight, normalized weight, source/target types and dataset; denominators include ALL source outgoing edges before target selection; absent annotation for external targets must not exclude those weights; no-outgoing selected nodes retained; reject invalid weights/IDs, duplicate edge identities and wrong provenance/schema; no input mutation; deterministic identity hashes/counts and required graph manifest fields; tiny fixture demonstrates invariant normalization under subgraph change and reproducibility under row-order change. Test malformed inputs and empty graph.
- Scope: bounded extraction API over caller-supplied confidence-filtered source rows; document completeness as an input precondition (all outgoing rows for selected sources, not prefiltered targets). Do not claim automatic full-source verification, bulk loader/cache, graph runtime API, or biological validation. P1-04/P1-05 remain separate.
- Validation: entire unit suite Windows/Thor. PM may independently build small real-source outgoing fixture from existing Thor official cache and run API if feasible; no raw dataset in Git, no credentials. Synthetic evidence must be labeled synthetic.
- Handoff: precise API, tests/results, files, head SHA, provenance contracts/limitations. Commit branch only; PM handles final evidence, independent review and PR; stop at PR creation, no merge/phase approval.
