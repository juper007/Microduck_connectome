# P1-02 Task Packet

- Task/phase: P1-02 Annotation schema / Phase 1. PM READY → IN_PROGRESS; WBS has no individually assigned READY rows. P1-01 merged via PR #8; normalization is next independent data prerequisite.
- Owner: connectome-data-engineer. Reviewer: independent-phase-reviewer.
- Base: freshly fetched origin/main `6fa4680adcf964402061c1f8e0f8dfd55296eca1`; clean prior tree. Branch `feature/p1-02-annotation-schema`; isolated `work/p1-02-annotation-schema`.
- Goal: deterministic validated annotation metadata for pinned MaleCNS records, usable by subsequent graph extraction.
- Read first: AGENTS.md; own SKILL.md; WBS P1-02; data/repro policy §7 Body-ID policy and §13 credentials; existing microduck_connectome package. Token policy/context-map data-engineer row as needed.
- Inputs: explicitly defined mapping rows with neuPrint-style bodyId, type, instance, somaSide, class fields; pinned male-cns:v1.0. Document supported projection (not arbitrary neuPrint schemas). Synthetic fixtures must be labeled synthetic, no fabricated real biology.
- Outputs: small stdlib annotation schema/normalizer module; boundary tests; schema/usage documentation and compact validation handoff.
- Acceptance: preserve canonical positive integer body IDs (reject bool/float/string); retain source type, instance, side, class and dataset version; explicit optional/unknown semantics; never infer laterality from ID or instance; deterministic sorting by body ID; reject duplicate IDs and malformed/ambiguous records clearly; reject wrong dataset; JSON-safe deterministic serialization; tests demonstrate valid roundtrip and failure boundaries. Do not mutate input rows or silently discard relevant malformed values.
- Design: define/justify precise schema and source-side vocabulary from available evidence; distinguish soma side from functional/projection laterality. Avoid guessed field semantics. Unknown optional metadata may remain null; reject unsupported values rather than guessing. Source metadata/provenance must remain traceable through normalization.
- Scope exclusions: no graph extraction, network client changes, credentials, biological population claims, robot commands, new dependencies or unmerged PR #6 code imports. No full data downloads. No blanket docs preload.
- Budget: <25K input target; medium effort; seed N/A. Expand exact source/spec only for a concrete uncertainty and record why.
- Handoff: files, validation command/count, commit SHA, schema assumptions, limitations. PM validates on Thor, obtains independent final-head review and creates PR. Stop at PR; state REVIEW, no phase gate claim.
