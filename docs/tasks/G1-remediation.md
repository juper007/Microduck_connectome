# G1 remediation Task Packet

- Scope: resolve Phase1 gate findings F1–F3; related tasks P1-02/P1-03/P1-05. No biological Phase2 validation claim.
- Base: freshly fetched main72ffb1bf6921ce36d7c5073c1a97a8a15226f881; branch feature/g1-pathway-rebuild. Dedicated query implementation in feature/g1-pathway-queries, integrated by PM after commit.
- Owner: connectome-data-engineer; independent reviewer: fresh independent-phase-reviewer.
- Read first: AGENTS/own skill; COMPLETION_CRITERIA G1; PROJECT_EXECUTION_PLAN Phase1/2 only; neural spec§§4–5; current data/graph modules. Target <30K per implementation role; high only for final gate review.
- F1 acceptance: deterministic bounded directed shortest/restricted path search (hop/type/node/weight restrictions documented); source-backed descending and cross-region connectivity query, unknown anatomical evidence explicit. No laterality/region inference from ID or type prefixes. Existing weight/provenance/view isolation preserved; failure tests and fixtures required.
- F2/F3 acceptance: portable committed CLI/source adapter + pinned source/download/hash manifest + selection config; candidates inventory and selected LC10a→DNa02 two-hop graph report reproducible from full official source. Intermediate nodes limited to annotated neurons; all source outgoing edges contribute denominator before graph selection. Candidates LC4,LPLC2,DNa02,DNp01,DNge104,MDN searched by exact names, absent names explicit not aliases. Report observations only, hypotheses/uncertainty separate. Choose actual metadata vocabulary by inspecting pinned source, never guess. Raw source side fields preserved with explicit L/R adapter.
- Outputs: source manifest/config, modules/CLI/tests/docs; generated graph/cache outsideGit; small candidate/pathway report and graph/hash manifest versioned, including source/confidence/config/code SHA and selection semantics. No hardcoded local paths; arguments for cache/output, optional verified downloads. No credentials required for public flat source. If neuPrint annotation limit differs, state source population definition.
- Reproduction: fresh checkout/locked env onThor; two deterministic runs same source/config produce matching hashes/counts; independent review must rerun committed command from raw hash-verified inputs. Regression complete suite; fresh reviewer gates actualPhase1 criteria not narrowpacket alone.
- Do not alter frozenlimits/dataset/neuralmodel; no actuator commands. No unrelated docs preload or new dependencies beyond existing pyarrow/numpy. No main edits/directpush.
- PM assembles commits, runs source validation, adds evidence, requests independent finalhead G1 review and creates PR. Merge not authorized by this turn. Review verdict may remainFAIL/BLOCKED if realrequirements unmet.
