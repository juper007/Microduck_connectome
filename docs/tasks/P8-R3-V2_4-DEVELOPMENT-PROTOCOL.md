# P8-R3 V2.4 prospective development protocol

Status: **preregistered before any V2.4 gate execution or official seed**. This version resolves the V2.3 pre-execution criterion conflict; it does not change or reinterpret earlier results. Input branch `feature/p8-r3-resolution-independent-looming` at `584f9573949153db8f5a4e0a5cfef7e1dd467114`; original fetched `origin/main` base `e4ade0276e5e3f67381f4ca3da9f181077387fa1`. Freeze a new V2.4 source/protocol/config/metric-extractor commit and raw paths before the first V2.4 gate run. Any result-driven change needs a new protocol version and unused seeds. Keep all attempts and raw artifacts.

## Retained evidence and the corrected prospective question

V2.2 official Thor development at seeds **885521–885523** remains **0/3 FAIL**: 4/3/2 armed startup neural ticks had `input_none=true, result_none=true` despite later healthy preboundary stops. V2.3 introduced validated first-frame insertion into the scheduler perception cache. Its local and Thor 11-case raw gate records remain intact, but an [independent exact-head review](../evidence/p8-r3/v23-preflight-review-fail.md) of `584f957` returned **FAIL for official promotion**. The V2.3 protocol's unqualified “any skipped armed tick fails” rule conflicted with the deliberately dropped-frame case, which retained seven missing ticks and was labeled PASS for authentic safe-stop behavior. No V2.3 official seeds **885621–885623** were run. Preserve these negative and blocked outcomes; do not retroactively score V2.3 as a successful preregistered gate.

**V2.4 hypothesis:** the unchanged V2.3 cache-priming runtime can provide complete healthy startup input, while an intentionally induced later loss of RGB must create visible missing-input records and a persistent fault stop. These are **two different experiments with different denominators and pass rules**. A V2.4 focused gate is PASS **if and only if both groups below independently pass** under the newly frozen V2.4 script/schema. A healthy-path failure cannot be offset by a safe fault outcome, and an injected fault cannot be relabeled as healthy behavior. This is an engineering scheduler and safety claim, not biological evidence.

## Source reuse and fixed factors

Reuse the V2.3 priming implementation from source `fde86b36d54ad652a8c93e3f043563a24e5b8fd6` **only after** an independent diff and hash audit proves that the V2.4 candidate retains the same runtime semantics: the already validated neutral priming PerceptionFrame is inserted into the scheduler cache before workers start, with original camera timestamp and frame ID; no timestamp rebase, duplicate direct graph step, robot-facing priming command, or stale/invalid-frame acceptance. New V2.4 gate code, schema, config, and official seed/path metadata must be separately versioned and frozen before execution. If runtime behavior changes, rerun affected synthetic/fault evaluations under a new prospective freeze rather than treating V2.3 evidence as proof. V2.2's [18-case fractional RGB internal screen](../evidence/p8-r3/v22-internal-development-summary-v1.md) and fault-stop gate may be reused only when their renderer, estimator, graph, decoder, Watchdog, fault arbiter, and transport code/config hashes and the source diff prove those paths unchanged; document that proof in the V2.4 evidence.

Keep the 8×8 fractional renderer and detector, V2 A log-area estimator (epsilon `1e-6`, full-scale rate `0.5 s^-1`, `>150 ms` estimator reset), **20 Hz RGB / 50 Hz neural and control**, unchanged scenario/geometry, graph-v2 SHA256 `c4160c42941163079b6b569d117bf67afcf8b45eaa128c444f7c96c2567593cc`, MaleCNS identities/weights, neural model, SensoryMapper, EscapeDecoder threshold **0.5**, evaluator-only center-distance boundary **0.25 m**, SafetyClamp, Watchdog and **100 ms perception TTL**, **500 ms robotd deadman**, fault-stop priority/persistence, `robot.stop` transport, and robotd motor authority fixed. Do not retune gain, thresholds, cadence, safety limits, boundary, graph, or stop policy.

## Group A — healthy startup handoff gate

In a deterministic 20 ms tick case and a real threaded scheduler case at the frozen V2.4 source, use valid continuous 20 Hz RGB and a validated neutral first frame. The direct neutral graph/Watchdog priming update occurs exactly once. Insert that same frame into the scheduler cache before starting workers; retain its source timestamp and frame ID and issue no robot-facing command during insertion. For **every armed neural tick** in these healthy cases, including the first worker tick and ticks through simulated stop confirmation/deadline, require `input_none=false`, `result_none=false`, valid perception, a present original-source timestamp, measured frame age in **[0, 100] ms**, finite healthy update, and no dropped neural tick. Consecutive actual RGB frame gaps must be **≤100 ms**. The first ordinary visual frame must replace the primed cache; neural sequences must be contiguous with no duplicate direct priming step. The real worker must have zero scheduler exceptions, no positive move after a stop latch, and acknowledged `robot.stop` refresh with maximum ACK gap **100 ms** through its observation endpoint. **One missing or stale armed tick in either healthy case makes Group A FAIL**, regardless of later stops or Group B results. Report all tick counts, missing counts, ages, frame gaps, and ACK gaps, not maxima over a filtered subset.

## Group B — deliberate fault-injection safety gate

Use separately labeled cases for absent, invalid, future-dated, stale, malformed, and NaN priming frames, a delayed scheduler start, a sustained subsequent visual drop that crosses the unchanged 100 ms TTL, and an external fault after a healthy neural stop. Priming rejection and delayed-start cases must fail closed before scheduler worker publication: preserve attempted source timestamp, rejection reason, and **zero** robot-facing commands. A delayed handoff after arm is an **armed failure** for an official trial even if this focused rejection test passes as a safety test.

In the sustained visual-drop case, record **every** missing or stale neural tick, its timestamp, frame age when computable, and the first invalidation and fault times. The count is expected to be **nonzero** and must remain in the raw denominator. Require authentic `stale_neural`/fault safe-stop activation after the loss, `robot.stop` ACK and continuing refresh with maximum gap **100 ms**, zero positive `robot.move` request or ACK after first stop latch, and zero scheduler exception. Fresh later neural/visual data must **not** resume motion. In the neural-then-external-fault case, the effective fault/arbiter latch reason must supersede neural priority while preserving Watchdog's first-fault history; every subsequent robot-facing publication must remain `robot.stop`. **Any failure of rejection, fault detection, priority, persistent stop, ACK refresh, or no-resume makes Group B FAIL.** Deliberate missing ticks are evidence of the injected fault and do not enter Group A's healthy zero-missing denominator. Group B passing is a safety result only, never a healthy looming result.

Run the complete new V2.4 case matrix and extractor on local Python and Thor Python 3.12 before official seeds; the planned categories are the two Group A cases and nine Group B cases above (**11 cases per environment**). Freeze case names, timing, expected fault injection, schema, threshold checks, command/ACK extraction, and output paths in the V2.4 script/config before either execution. Keep both raw outputs, per-case event ledgers, hashes, byte/record manifests, exact commands, and Python versions. The independent reviewer must verify **Group A PASS and Group B PASS separately** on the final source head. A summary-only PASS label cannot replace per-event verification. If either group fails, official recertification is blocked and all results remain retained.

## Fresh official development recertification

Only after both V2.4 focused groups pass, reuse conditions are proven, and an independent pre-execution review approves the exact frozen source/protocol/config, run **885721, 885722, 885723** in that order as three full independent official duck-sim + robotd + MuJoCo resets on Thor. Assign approach arm elapsed times **2.0, 2.6, 3.0 s**, respectively. Keep the V2.2/V2.3 65×33 official geometry, policy, moving precondition, observation windows, 20 Hz visual cadence, upstream pins, and all safety and metric rules fixed. Freeze a new raw root/attempt IDs; these seeds are disjoint from all V2.1–V2.3 development seeds and P8 final ranges `880000–880119`, `881000–881023`, `882000–882023`, `883000–883009`. Do not run 885621–885623 as a substitute or pool versions.

For **every** armed official neural ledger row, including priming and every scheduler tick through pose-confirmed stop, require non-None input/result, valid perception, preserved real RGB timestamp, age **0–100 ms**, finite healthy graph state, and no missed/stale neural update. Require actual consecutive RGB gaps **≤100 ms**, visual lineage within **five** 50 Hz runtime steps, no scheduler exception, and complete causal RGB → fractional area → A looming → LPLC2 → graph-v2 DNp01 ≥0.5 → EscapeDecoder → SafetyClamp/healthy Watchdog → neural stop latch → `robot.stop` request/ACK → applied-vx decline → pose-derived stopped confirmation. First healthy stop and first stop request must occur at strictly positive **0.25 m** boundary margins while the body is moving with fresh positive applied vx. Stop refresh maximum ACK gap remains **100 ms** through confirmation before deadman/cleanup. Require 3/3, zero safety-limit violations, zero fault/deadman/scripted/shutdown cessation confounds, and zero post-latch positive moves. A fault stop is a safety outcome but official behavioral FAIL. These three runs are development-only and cannot count toward P8-02 final statistics or establish G8.

Only an exogenous **pre-arm** failure preventing a comparable trial may be invalid, with its attempt and reason retained. An armed moving trial with any missing neural input, stale age, late/no healthy stop, boundary crossing at trigger/request, fault, deadman, transport gap, scheduler exception, failed stopped confirmation, or safety event is FAIL. Repeated pre-arm invalidity blocks recertification. Complete the planned three-reset matrix; never selectively replace an adverse armed run. Any repair after outcomes requires another prospective version and fresh official seeds.

Archive raw per-case/per-seed records, all rejected hypotheses, source/upstream/config hashes, exact commands, SHA256/byte/record manifests, timing and boundary margins, simulator cleanup, and known limits under `docs/evidence/p8-r3/`. Independent exact-head review must verify the separate gate denominators, source-diff reuse, prospective freeze, unchanged graph/decoder/boundary/TTL/safety, complete 3/3 official causality, raw hashes, and current-main freshness before any merge or phase-completion claim. G8 and P8-02 final remain unearned by this development protocol alone.
