# G8-R5c — isolated moving-body neural-stop causal recertification

- Base: freshly fetched `origin/main` `c7f23ac21da9d089d663e2c216cc5a07cb834b6c`.
- Branch: `test/g8-r5c-isolated-neural-stop`; isolated local and Thor worktrees.
- Owner: `microduck-integration-engineer`; safety support: `robot-safety-engineer`; final reviewer: `independent-phase-reviewer`.
- Read first: this packet, `config/g8_r5c_neural_stop_v1.json`, frozen P6 motion/scheduler/telemetry configs, `scripts/p6_telemetry_runtime_fixture.py`, P8-01 scenario contract, graph-v2 manifest, and the G8-R5b failure summary. Do not preload Phase 9 or R1–R4 raw evidence. Context budget <50K input tokens; medium implementation, high safety/review reasoning.
- Goal: in three fresh official Thor robotd/MuJoCo resets, show the unchanged graph-v2 looming path causes `robot.stop` while the actual body is still moving. Require the first observed pose-stop sample before the refreshed 500 ms deadman deadline, then sustained confirmation; later deadman events are retained. The bounded positive precondition is an engineering test setup, not MaleCNS output.
- Fixture isolation: suppress only authentic watchdog-minted exact zero-twist non-stop outputs at the G8-R5c publisher wrapper. Preserve each suppressed output and same-cycle neural trace in events. Any nonzero non-stop output fails the trial. Production scheduler, adapter, graph, decoder, safety, watchdog, and robotd remain unchanged. This mechanism is not P8 final behavior.
- Two-stage fixture lifecycle: the full P6 scheduler and neural chain run until the first official neural `robot.stop` ACK. The fixture-only scheduler then joins its workers without publishing a shutdown stop. Any in-flight visual frame after ACK is discarded before graph input and logged; every completed graph call must precede ACK. The visual scenario ends at ACK; its world center is held only for evaluator clearance checks. Stage 2 reads official robot.state and MuJoCo pose only until actual sustained cessation, emits `COMPLETE`, and then issues cleanup stop. A timeout/fault instead sends a safe abort stop and fails. This handoff is test isolation, not production scheduler behavior or a continuous-approach P8 trial.
- Inputs: graph-v2 SHA256 `c4160c42941163079b6b569d117bf67afcf8b45eaa128c444f7c96c2567593cc`; frozen P8-01 visual generator and three seeds; unchanged P3–P6 configs and walking policy. PR #60 and #61 remain historical FAIL evidence, unmerged.
- Protocol: development probe may adjust the **uncommitted draft** before final run. Commit the exact protocol and source before any final batch; thereafter no threshold or fixture changes under this version. `config/g8_r5c_neural_stop_v1.json` is authority for exact numeric bounds once frozen.
- Outputs: versioned protocol, fixture/metric tests, immutable Thor raw trace/events with SHA256, concise evidence summary, separate PR.
- PASS: all three independently reset trials have confirmed positive motion; a fresh pose-speed sample still proves motion immediately before first neural stop, and fresh robot.state applied vx is at least 0.04 m/s. A complete ledger of consecutive healthy graph-v2 neural steps must link the latest positive looming/LPLC2 visual step to the first DNp01 threshold crossing within 100 ms and at most five runtime steps, with no invalid/stale intermediate input. The stop step may have zero current looming because the reservoir integrates earlier input. LC10a is a co-input, so this is bounded temporal visual lineage, not unique LPLC2 biological causation. Decoder, SafetyClamp, and healthy Watchdog must reach unchanged `robot.stop`; official stop ACK precedes actual sustained cessation. The last positive move call-start→stop-ACK excludes the 500 ms deadman with frozen margin. Requested twist becomes exact zero after stop; applied vx begins material decline before the old positive-command deadline and crosses the frozen near-zero threshold before the refreshed stop-command deadline. The first actual pose-stop sample must precede that refreshed deadline without a deadman limit; 200 ms sustained confirmation may finish later. No intervening robot-facing command, safety violation, or scheduler exception is allowed. An actual `COMPLETE` transition is required. One failure stops the batch without replacement, and the batch manifest must retain trial exceptions and artifact hashes. A sphere clearance breach fails. This supports stop-initiated deceleration and eventual cessation in the isolated fixture, not a continuous-approach P8 boundary result.
- Review and integration: exact final PR head and current main freshness; PASS-only merge. G8-R6, P8-v2, and G8 final gate are outside this task.
