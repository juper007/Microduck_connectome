# G8-R5d — production stop-refresh moving-body neural-stop recertification

- Base: fetched `origin/main` `c7f23ac21da9d089d663e2c216cc5a07cb834b6c`.
- Branch: `test/g8-r5d-production-stop-refresh`, isolated worktree.
- Owner: `microduck-integration-engineer`; safety support: `robot-safety-engineer`; reviewer: `independent-phase-reviewer`.
- Context budget: <50K input tokens, medium reasoning. Read first: this packet, R5c task/failure summary, P6-03 stop semantics, current scheduler/watchdog/adapter/client, graph-v2 manifest, P8-01 looming scenario contract. Do not preload Phase 9+ or R1–R4 raw evidence.
- Goal: three independently reset official Thor robotd/MuJoCo trials establish moving body, fresh graph-v2 looming→LPLC2→DNp01 lineage, healthy neural escape stop, repeated acknowledged `robot.stop` at production watchdog cadence, actual applied-vx decline, sustained pose-derived stop, and no deadman limiter before confirmation.
- Inputs: unchanged graph-v2 SHA256 `c4160c42941163079b6b569d117bf67afcf8b45eaa128c444f7c96c2567593cc`, frozen P3–P6 configs, official MicroDuck walking policy, P8-01 scenario, seeds in `config/g8_r5d_stop_refresh_v1.json`.
- Isolation: bounded official high-level `robot.move` is an engineering test precondition. Before the first stop only authentic exact-neutral non-stop output may be suppressed. After first stop ACK, fixture ends visual/neural production, while unchanged watchdog/control tick and `RobotMotionAdapter` continue official `robot.stop` refresh. Any later `robot.move`, including zero twist, fails. The held virtual sphere center is evaluator-only.
- Frozen protocol: the new versioned config is committed before the final batch. Development probes are excluded from final evidence. The final batch cannot change thresholds or code based on results.
- Outputs: fixture/metrics/scripts/tests, committed protocol, three fresh-reset trial artifacts and hashes on Thor, concise evidence summary, separate PR. R5/5b/5c PRs #60/#61/#62 remain immutable failed evidence.
- Acceptance: every started trial independently meets every frozen check, including fresh moving pose speed >=0.015 m/s, pre-stop applied vx >=0.04 m/s, stopped pose speed <=0.008 m/s sustained for 200 ms, positive neural lineage and healthy first stop, >=2 acknowledged stop requests, ACK gaps and final tail <=100 ms (<500 ms deadman), zero deadman limiter through stopped confirmation, zero post-stop robot.move, zero sphere breach, zero scheduler exceptions and safety violations, actual `COMPLETE` event. Any started failure ends the batch with raw evidence retained and no replacement.
- Review: exact final head independent review; sync current main, rerun affected verification, and rereview after any post-review commit. Merge only on final PASS. G8-R6, P8-v2, and final G8 review are outside this task.
