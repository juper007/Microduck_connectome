# P3-01 Task Packet — Define neuron model

```yaml
task_id: P3-01
phase: 3
title: Freeze executable MVP neuron-model contract
owner_skill: neural-runtime-engineer
reviewer_skill: independent-phase-reviewer

base:
  repo_commit: bb3163b6ece6f3d71a33f10971450a84c2171497
  dataset: male-cns:v1.0

goal: >-
  Turn the frozen P-1.1 LIF-like neural model specification into one versioned,
  machine-validated configuration contract that P3-02 can implement without
  reinterpreting update order, weight semantics, or MVP invariants.

read_first:
  - docs/TASK_BREAKDOWN.md#P3-01
  - docs/preflight/NEURAL_MODEL_SPEC.md#2-mvp-model-class
  - docs/preflight/NEURAL_MODEL_SPEC.md#3-default-parameters
  - docs/preflight/NEURAL_MODEL_SPEC.md#5-mvp-weight-normalization
  - docs/preflight/NEURAL_MODEL_SPEC.md#10-timing-and-deterministic-behavior
  - .agents/skills/neural-runtime-engineer/SKILL.md

do_not_preload:
  - perception implementation
  - robot integration internals
  - experiment-result logs
  - unrelated neuroscience literature

inputs:
  - frozen neural model spec P-1.1
  - Phase 1 graph contract with full-source normalized weights

outputs:
  - config/neural_model_v1.json
  - microduck_connectome/neural_model.py
  - tests/test_neural_model.py
  - docs/NEURAL_MODEL_RUNTIME.md

acceptance:
  - all P-1.1 default parameters are explicit in a versioned config
  - synchronous update order and previous-step spike semantics are unambiguous
  - unsigned recurrent weights and full-source normalization invariants are explicit
  - config validation rejects non-finite, type-invalid, plastic, nondeterministic, or contradictory MVP settings
  - canonical config hashing is deterministic and sensitive to parameter changes
  - no sparse graph execution, sensory API, readout implementation, or robot command is introduced
  - focused tests pass

context_budget: "<25K input tokens target"
reasoning_effort: high

handoff:
  include:
    - files changed
    - tests and results
    - canonical config SHA256
    - newly discovered dependencies
    - known limitations
```

## PM decision

P2-01 through P2-05 are merged and independently reviewed, so P3-01 is the next WBS task with satisfied prerequisites. The implementation must preserve the frozen P-1.1 model rather than redesign it. P3-02 owns sparse execution; P3-03 owns stimulus injection; P3-04 owns population readout.
