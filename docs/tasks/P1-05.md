# P1-05 Task Packet

- Task: P1-05 Graph API / Phase 1. PM READY → IN_PROGRESS after merged P1-04 (#11). No separately assigned READY issue.
- Owner connectome-data-engineer; reviewer independent-phase-reviewer.
- Base freshly fetched origin/main `20f6b494b0e09678eb8ddc59a03209eb0f0c4a53`, clean before branch; feature/p1-05-graph-api, isolated work/p1-05-graph-api.
- Goal: deterministic read-only query API over validated P1-03 artifacts for future neural runtime use.
- Read first: AGENTS/own skill, WBS P1-05, NEURAL_MODEL_SPEC §§4–5; current connectivity and graph_cache public APIs/docs only as needed. Lazy <25K context, medium. No robot/perception/bulk dataset context.
- Inputs: validated full artifact or cache directory plus expected key; committed P1-03 real graph is integration fixture.
- Outputs: small stdlib graph module, focused tests/docs, validation handoff.
- Acceptance: reuse existing validation (avoid new divergent schema); deterministic body-ID ordering; lookup node and source→target edge; incoming/outgoing neighbors or edge lists with raw+normalized weights; exact source type population selection including explicit unknown semantics; induced read-only view on selected IDs retains isolated nodes, exact full-source sums and unchanged normalized weights; unknown IDs and invalid types handled explicitly (bool not an ID); input/returned mutations cannot corrupt internal state; root provenance/content identity retained through nested views; empty/singleton/self-loop/directed cases tested; cache-backed load respects expected key; malformed artifact rejected.
- View semantics: graph queries see only edges present in artifact/current view; do not claim whole MaleCNS connectivity. Nested subgraph cannot reintroduce excluded nodes. Preserve parent source provenance; do not relabel parent manifest counts/hashes as independently extracted view metadata. No need to serialize views into extraction-artifact schema or introduce a competing persistent graph format.
- Constraints: no normalization recalculation, biological side/function inference, neuron dynamics, bulk acquisition, dependency changes or motor commands. P1 completion gate remains independent, not implied by this API.
- Validation: focused tests locally; PM whole suite on Thor Python3.12.3 plus real graph/cache/view integration. Seed N/A unless explicit deterministic property probe.
- Handoff: stable API early, files/test counts/commit and limitations. Commit branch only; PM manages independent final-head review and PR. Stop on PR creation, no merge.
