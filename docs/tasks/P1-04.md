# P1-04 Task Packet

- Task/phase: P1-04 Cache/provenance, Phase 1; PM READY → IN_PROGRESS after merged P1-03 (#10). No individually assigned READY issue exists.
- Owner: connectome-data-engineer; reviewer: independent-phase-reviewer.
- Base: freshly fetched origin/main `df43894143e15ec4cfe189764651ea3e19621cbe`, clean before branch; feature/p1-04-cache-provenance, isolated work/p1-04-cache-provenance.
- Goal: trustworthy local roundtrip cache for P1-03 derived graph artifacts, preserving full provenance and rejecting corrupt or mismatched cached data.
- Read first: AGENTS/own skill; WBS P1-04; DATA_AND_REPRODUCIBILITY_POLICY §§4–6,8,12; connectivity.py and its artifact docs, annotations.py only as needed. Target <25K, medium. No full docs/raw data/robot context.
- Inputs: JSON-safe P1-03 graph including annotations, edges, outgoing sums, manifest; caller-selected cache directory; expected provenance/identity at read time. Use committed P1-03-real-graph.json as a small integration fixture.
- Outputs: stdlib cache API; boundary/roundtrip tests; focused contract/usage doc and validation handoff.
- Acceptance: deterministic content/identity key incorporates dataset/schema/source and extraction configuration (document timestamp policy); changed source or config cannot hit old identity; retain entire provenance; verify bytes/digest and expected identity on load; reject malformed or unsupported graph/schema, nonfinite values, duplicate JSON keys and path-like keys; safe atomic publish, interrupted write cannot expose partial final artifact or destroy previous good entry; corruption never silently returned; no changes to input artifact; tests cover roundtrip, invalidation, corruption, failure injection and real fixture. Validate graph internal hashes/counts/normalization where recoverable from artifact; do not imply verification of original source completeness.
- Scope: bounded trusted-local-filesystem cache for small derived artifacts, no downloading, raw-source cache migration, credentials, garbage collection, bulk graph API or hostile shared-directory hardening. No new dependencies. Do not weaken P1-03 normalization/provenance contracts.
- Design: prefer narrow content-addressed immutable entries over broad indexing service; no need semantic cache hit across changed creation timestamps if explicitly documented. Source graph completeness/upstream authenticity remain caller assertions. Distinguish byte integrity from biological validity.
- Validation: new/offline tests locally; PM full-suite and committed real graph roundtrip on Thor Python3.12.3. No simulator commands or environment modification.
- Handoff: stable API early, files/commands/counts/head/known limits. Commit only branch; PM manages independent final-head review and PR. Stop after PR creation; no merge or phase approval.
