# P8-02 R1 prospective remediation protocol

Status: **preregistered design; no B seed executed**. Task `P8-02-R1`, branch `docs/p8-02-r1-remediation`, fetched `origin/main` base `dbade36ac62e065441c554b7715662410872122f`. The companion [machine-readable matrix](../../config/p8_02_r1_protocol_v1.json) is authoritative for trial IDs and thresholds. Freeze a clean, independently reviewed execution head and R1 harness/config hashes before the first B trial. This protocol repairs acquisition order and evidence durability; it does not change the selected controller, scientific gate, or safety limits.

## Retained result and hypothesis

The [earlier P8-02 evidence](../evidence/p8-02/final-approach-summary.md) remains **protocol FAIL**. Its later 20/20 healthy preboundary stops and zero safety violations are mechanical observations, not a valid P8-02 gate: A00–A07 had ID-assigned attempts under an operator-selected wrong `microduck_rl` checkout, and the aborted batch lacked a contemporaneous journal and JSON manifest. Those are not permitted exogenous pre-arm retries. Preserve all A raw and summaries without recoding or pooling. Eleven of the later 20 pose-confirmed stops were preboundary; nine crossed the evaluator boundary during deceleration. R1 tests whether a new, fully accounted prospective 20-trial batch meets the same healthy causal stop rule.

## Frozen selection and matrix

Keep the merged P8-V2 final protocol and `config/p8_02_final_approach_v1.json` V2.4 selection: 8×8 fractional RGB renderer/detector; log-area V2 estimator A (`epsilon=1e-6`, full-scale `0.5 s^-1`, reset after `>150 ms`); **20 Hz visual**, **50 Hz neural/control**, **100 ms** freshness TTL; graph-v2 SHA256 `c4160c42941163079b6b569d117bf67afcf8b45eaa128c444f7c96c2567593cc` from `male-cns:v1.0`; DNp01/EscapeDecoder threshold **0.5**; **0.25 m evaluator center-distance boundary**; unchanged SafetyClamp, Watchdog, latches, 500 ms robotd deadman and `robot.stop` transport. Robotd retains motor ownership. Keep MicroDuck commit `344925c9f8fa031f85428a305b1e8ec2eaae29c1`, microduck_rl commit `cb70b792312d559a4da09064d92009079671815f`, policy SHA256 `98c3ea73fa6bb196bcf16586cf38b9fffb782787447d638fa1c1028df9082c1a`, and scenario SHA256 `3967c2efa327c0a69a6f9629a12ac0c80f38a7ca6b02b4e9aceecd4cbd53c9eb`.

Run exactly **20 independent fresh official Thor duck-sim + robotd + MuJoCo resets**, in order **B00–B19**, seeds **880020–880039**, with the unchanged 2.0/2.6/3.0 s arm-time cycle and 0.20 m/s approaching virtual sphere. Seeds 880040–880119 remain unused and are not replacements. Require measured moving body and fresh positive applied vx before arm. The gate remains **≥19/20** healthy graph-v2 DNp01→EscapeDecoder→SafetyClamp→Watchdog neural stops and first `robot.stop` requests while moving and strictly before 0.25 m, with **zero safety-limit violations**. Every assigned ID has one scored armed result. Fault, deadman, scripted stop, missing/stale armed visual or neural input, late/absent request, ACK/transport failure, invalid causal lineage, scheduler exception, or unconfirmed pose stop is an armed failure, never a replacement. Require visual gaps and neural source ages ≤100 ms, lineage ≤5 runtime steps, stop ACK refresh gap ≤100 ms, and no positive move after latch.

Before any B final seed, run a **separate, prospective development gate** required by the master §30 on three unused distinct seeds: `D00=886020` at 2.0 s arm, `D01=886021` at 2.6 s, and `D02=886022` at 3.0 s. Repository reference search found no prior use of these seeds at preregistration. Use three fresh official Thor resets under the reviewed R1 harness after the same complete pre-output preflight barrier, with the selected controller and B-like causal/transport/raw accounting rules. Development PASS requires **3/3** healthy moving-body preboundary neural stops and first requests, **zero** safety-limit violations, complete incremental journal/SHA256-byte-record manifest, verified final down/port state, and passing focused demonstrations that an operator path/commit mismatch aborts with zero assigned IDs and an explicit audit log, SIGINT preserves a checkpointed attempt, and SIGKILL recovery inventories untouched raw without an armed retry. The fault demonstrations use synthetic or pre-arm no-seed setup, never alter D/B outcomes; their artifacts must be separately retained. An armed D failure or evidence gap is a development FAIL and **blocks all B seeds** until a separately versioned remediation. Retain all negative D/preflight evidence. D results are never pooled into the B denominator or used to choose B seeds; the B matrix and thresholds stay fixed.

Record per B ID the first positive looming, LPLC2, threshold DN, healthy neural stop, first stop request/ACK, applied-vx reduction/near-zero, and pose-confirmed stop timestamps. Extract looming→neural stop, neural stop→request, neural stop→ACK, ACK→pose stop, and looming→pose stop latencies in elapsed milliseconds; report median, nearest-rank p95, maximum, per-ID outcomes and Wilson 95% CI. Report evaluator center distance and **separate trigger, request, ACK and pose-confirmed-stop boundary margins**. Preboundary pose cessation and virtual surface clearance are secondary, non-gating outcomes. A preboundary trigger/request does not imply a preboundary stopped body; the sphere has no MuJoCo contact geometry.

## Preflight barrier before output or trial assignment

The R1 batch harness must validate the entire environment **before creating either development or final batch output directory, an attempt folder, a trial row, or selecting a trial ID**. Run the barrier separately for D and B batches. Resolve and compare operator-supplied absolute Thor source, MicroDuck, microduck_rl, graph-artifact, walking-policy, simulator executable, protocol, and output/state paths against the reviewed execution config. Verify clean exact reviewed source HEAD, committed protocol/harness bytes, pinned upstream HEADs, graph and policy SHA256, scenario/config hashes, robotd deadman and policy requirements, official Thor Python 3.12 and isolated state/port. Record exact resolved paths, commands, hashes, UTC time and result in a standalone **preflight audit log outside the uncreated batch output directory**. A path, commit, hash, policy, executable or source mismatch is an **operator/preflight abort with zero assigned trial IDs**. Log, correct and rerun preflight; never call it an exogenous pre-arm invalid trial or retrospectively exclude it. Recheck pinned inputs inside each trial. Preflight failure blocks seed execution. Pin the clean reviewed R1 source path/commit in the execution config before D00; the currently staged Thor checkout is not automatically the reviewed R1 head.

The **planned new isolated R1 source** is `/home/juper007/projects/microduck-connectome-thor/p8-02-r1-source`; create it only after the execution head is reviewed, and leave the shared `/connectome` checkout untouched. Thor paths already verified for MicroDuck, microduck_rl, graph and policy are `/home/juper007/projects/microduck-connectome-thor/microduck`, `/home/juper007/projects/microduck-connectome-thor/microduck_rl`, `/home/juper007/projects/microduck-connectome-thor/evidence/g8-r1/graph-v2/c4160c42941163079b6b569d117bf67afcf8b45eaa128c444f7c96c2567593cc.json`, and `/home/juper007/projects/microduck-connectome-thor/evidence/p7-02/policy-development-6012390-20260924/checkpoint1250-diagnostic/model1250.onnx`. Freeze and verify the isolated source's exact R1 execution HEAD before seed use. Reserved IDs in this document are a prospective plan; **assignment** means creating an active attempt/row during execution after the passing barrier.

## Durable attempt accounting

After each passing preflight, create a new batch-specific output root and atomically initialize its journal with all three D IDs or all 20 B IDs as `PENDING`, plus an empty SHA256/byte/record manifest **before starting D00 or B00**. Each ID gets unique `Dxx/attempt-01..03` or `Bxx/attempt-01..03` directories. Checkpoint journal and manifest after every acquisition step (down, up, policy load/readback, pose/motion precondition), arm transition, trial exit and cleanup. Each checkpoint writes a temporary file in the same directory, flushes/fsyncs it, atomically replaces the prior file, and fsyncs the directory. Inventory every extant raw/log file with SHA256, bytes and record count where applicable; record UTC/monotonic times, status transitions, exact command/exit/exception, validity class and source/config/upstream hashes. Never overwrite an earlier attempt.

Only exogenous simulator startup, IPC acquisition, fixture/pose reset, or measured-motion acquisition failure **before arm** may receive a same-ID, same-seed retry: at most **three total attempts**, all retained. Operator/config/path/commit/hash errors and harness bugs are not exogenous. If all three permitted attempts are pre-arm invalid, the batch cannot PASS; the ID remains planned. Once armed, an ID has **no retry** after any result or interruption. On SIGINT or catchable exception, stop safely, checkpoint current attempt and batch, hash extant files, perform final simulator down/port probe, and retain a machine-readable interrupted result. After SIGKILL/power loss, restart only in **audit/recovery mode**: scan untouched raw, hash/count it, mark the in-flight ID `INTERRUPTED_UNKNOWN_ARM` unless raw proves its state, and never rerun if arm occurred or cannot be ruled out. Never promote an unaccounted interrupted batch to PASS. Later unstarted IDs may continue only under the same frozen source/config and preserved journal.

## Decision and evidence

Final PASS requires the prior **3/3 D development PASS**, all 20 B IDs accounted and eligible armed, ≥19 healthy causal preboundary B successes, zero B safety-limit violations, all freshness/lineage/transport/stop rules, complete per-attempt raw/journal/manifest agreement, and independent exact-head review. Unresolved pre-arm invalidity, operator mismatch after assignment, missing raw/checkpoint, or unknown armed status blocks PASS even if a healthy subset is 20/20. Recompute all tables/latencies from raw JSONL. Retain exact commands, Python version, resolved paths, source/upstream/config/artifact hashes, policy readback, pose/visual/neural/decoder/Watchdog/command/ACK ledgers, stdout/stderr, checkpoints, manifest, scorer output and final down/port probe under separate new roots `/home/juper007/projects/microduck-connectome-thor/evidence/p8-v2-final/p8-02-r1-development-v1/` and `/home/juper007/projects/microduck-connectome-thor/evidence/p8-v2-final/p8-02-r1-approach-v1/`. Keep prior A evidence immutable.

R1 remediates P8-02 only. Independent review and PR merge of an R1 PASS precede P8-03; P8-04, final regression and G8 remain later gates. This engineering experiment does not establish MaleCNS topology superiority or authorize physical robot testing.
