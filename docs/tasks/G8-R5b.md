# G8-R5b — moving-body neural-stop integration recertification

- Base: freshly fetched `origin/main` `c7f23ac21da9d089d663e2c216cc5a07cb834b6c`.
- Branch: `test/g8-r5b-moving-neural-stop`; isolated local and Thor worktrees.
- Owner: `microduck-integration-engineer`, coordinated by `microduck-pm-architect`; independent final reviewer: `independent-phase-reviewer`.
- Read first: this packet, `config/g8_r5b_motion_stop_v1.json`, frozen P6 motion/scheduler/telemetry configs, `scripts/p6_telemetry_runtime_fixture.py`, P8-01 scenario code, graph-v2 manifest. Do not preload Phase 9 or prior raw batches. Context target <50K input tokens; medium implementation, high review.
- Goal: in at least three fresh official Thor robotd/MuJoCo resets, verify actual bounded precondition motion, then a healthy graph-v2 looming-derived stop transported through the unchanged P5/P6 stack, followed by measured motion cessation. The precondition is an engineering test setup, never a MaleCNS behavior output.
- Inputs: current graph-v2 SHA `c4160c42941163079b6b569d117bf67afcf8b45eaa128c444f7c96c2567593cc`; frozen P8-01 visual generator/config and three predeclared seeds; frozen P7 walking policy bytes; unchanged P3–P6 configs. PR #60 and its FAIL evidence remain historical and unmerged.
- Development basis before protocol freeze: official Thor short non-neural high-level robotd probe found 0.04 m/s selected `stand` with negligible displacement; 0.07 m/s selected `walk` with ~0.04 m trunk displacement over 1.5 s. Official `robot.state` exposes requested/applied twist but no measured odometry velocity in this setup, so the protocol preregisters pose-derived speed from official MuJoCo trunk x/y. The probe is not a final behavior trial.
- Outputs: versioned protocol, fixture/metric tests, Thor raw per-trial telemetry/events with SHA256, concise machine-readable summary and evidence README, separate PR.
- PASS: every independently reset trial reaches moving criterion before neural phase; pre-neural actual speed remains above frozen moving threshold immediately before first relevant healthy neural stop; LPLC2→actual graph-v2→DNp01 crosses unchanged escape threshold; decoder/SafetyClamp/healthy Watchdog preserve stop; official robot.stop acknowledgement precedes measured stopped confirmation; zero safety violations and scheduler exceptions. A startup/watchdog/manual/shutdown stop before neural stop, missing timing, stationary body, or policy mismatch fails. No P8 final gate claim.
- Freshness: exact final PR head reviewed, current main accounted for, PASS only merge. G8-R6 is outside this task.
