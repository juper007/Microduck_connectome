# P5-04 Task Packet — Safety clamp

```yaml
task_id: P5-04
phase: 5
title: Enforce the independent frozen motion envelope and fail-closed intent validation
owner_skill: robot-safety-engineer
reviewer_skill: independent-phase-reviewer

base:
  repo_commit: 1ce7c9ba8a0ee17acffbd2195b1ec49c0d1a604e

goal: >-
  Convert any decoder intent into a validated safe internal intent by enforcing
  frozen magnitude/slew limits, immediate stop override, and fail-closed malformed handling.

read_first:
  - docs/TASK_BREAKDOWN.md#P5-04
  - docs/preflight/INTERFACE_AND_TIMING_CONTRACT.md#12-mvp-motion-envelope
  - microduck_connectome/control_contracts.py

do_not_preload:
  - robotd integration
  - simulator transport
  - physical hardware

outputs:
  - config/safety_envelope_v1.json
  - microduck_connectome/safety_clamp.py
  - tests/test_safety_clamp.py
  - docs/SAFETY_CLAMP.md

acceptance:
  - abs(vx)<=0.08, vy=0, abs(vyaw)<=0.50
  - delta vx <=0.20/s and delta vyaw <=1.50/s using monotonic timestamps
  - stop=true immediately forces zero motion
  - NaN/Inf/bool/missing/wrong source/bad confidence/future or nonmonotonic metadata fail closed
  - fail-closed output is internal stop=true zero motion, never an unbounded command
  - clamp/failure reasons are explicit and deterministic
  - reset clears previous slew state
  - no robot command transport is introduced
  - supported Python 3.12 focused tests pass

context_budget: "<25K input tokens target"
reasoning_effort: high
```
